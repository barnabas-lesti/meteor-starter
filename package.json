{
  "description": "Basic application starter npm scripts.",
  "scripts": {
    "dev": "meteor --settings settings.dev.json",
    "staging": "meteor --settings settings.prod.json",
    "prod": "meteor --settings settings.prod.json"
  }
}
